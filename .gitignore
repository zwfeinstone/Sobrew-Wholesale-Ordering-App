# dependencies
/node_modules

# next build output
/.next
/out

# production
/build

# misc
.DS_Store
*.log

# environment variables
.env.local
.env
.env.*.local

# lockfiles (optional but recommended to commit normally — remove if you want lockfile tracked)
# package-lock.json
